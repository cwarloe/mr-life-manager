name: Validate repository content

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - "**/*.html"
      - "**/*.md"
      - "products/guides-pdf/**"
      - ".github/scripts/build_site.py"
      - ".github/scripts/check_built_site.py"
      - ".github/scripts/validate_repo.py"
      - ".github/scripts/check_pdfs_fresh.py"
      - ".github/scripts/check_entry_pages.py"
      - ".github/workflows/validate-content.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate HTML structure and local links
        run: python .github/scripts/validate_repo.py

      - name: Printables match their sources
        run: python .github/scripts/check_pdfs_fresh.py

      - name: Entry pages are complete
        run: python .github/scripts/check_entry_pages.py

      - name: Build and validate public site
        run: |
          python .github/scripts/build_site.py
          python .github/scripts/check_built_site.py
